# Contourf plots results

The important directory to consider is regular sampling. The first sampling for estimating the bitstring probabilities is a prefixed $n=10000$. The other directory won't be used for the thesis, but we kept here the results in case you want to see them.

More details of what it is $\texttt{regular\_sampling}$ and $\texttt{too\_big\_sampling}$ in $\texttt{../../result.ipynb}$.